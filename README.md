
## Прямая передача данных с метеостанций Ecowitt на сервер FROST

Прямая передача данных на сервер **FROST** с метеостанций и подключённых к ним датчиков **без использования локального хоста (Raspberry Pi / Home Assistant)**.  
Интеграция и стандартизация данных в соответствии с протоколом **OGC SensorThings API**.

---

## ⚙️ Аппаратные требования

### Мониторинговые устройства и сенсоры
- Метеостанции серии **GW** от *Ecowitt*.
- Любые датчики, поддерживающие режим **Custom Upload** ([инструкция][1]).

---

## 🖥️ Требования к серверу

| Компонент | Требование |
|------------|-------------|
| **Порт** | Открытый 8080 / 8081 для получения данных |
| **IP-адрес** | Статический внешний |
| **Доступ** | SSH с правами администратора |
| **Альтернатива** | Прямой терминальный доступ |
| **Метаданные** | Предварительно загруженные сущности: `Things`, `Sensors`, `Observations`, `Locations` |

### Сеть
- Стабильное подключение к сети Интернет.

---

## 💻 Системные требования

**Операционная система:**
- Ubuntu 20.04 LTS или новее  
- Debian 10 или новее  

**Программное обеспечение:**
- Python ≥ 3.8  
- Flask ≥ 3.0 — REST API-приёмник данных от Ecowitt ([2])  
- Requests — библиотека для HTTP-запросов ([3])  
- Gunicorn — WSGI-сервер для стабильной работы Flask-приложения ([4])  

**Инструменты для подключения:**
- WSL + Total Commander — для удобного доступа к серверу  
- Либо стандартный терминал / PowerShell  

**Мобильное приложение:**
- *WS View Plus* — для настройки интеграции мониторинговых систем ([5])

---

## 🧩 Настройка приёма данных на сервере FROST

Сервер **FROST** — реализация **OGC SensorThings API**, принимающая POST-запросы по REST API.  
Для корректной работы требуется промежуточный HTTP-сервис (Flask), который будет:
1. Принимать данные от метеостанций Ecowitt.  
2. Преобразовывать их в JSON-формат, понятный для FROST.  

---

### 1️⃣ Подключение к серверу и установка зависимостей

Подключитесь к серверу с помощью SSH (см. инструкцию по развёртке FROST).  
Рекомендуется использовать `screen` для стабильной работы.

```bash
sudo apt update
sudo apt install python3 python3-pip -y
